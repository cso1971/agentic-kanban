{
	"name": "@agentic-kanban/worker",
	"version": "0.1.0",
	"type": "module",
	"imports": {
		"#*": {
			"types": [
				"./src/*.d.ts",
				"./src/*.ts",
				"./src/*.tsx",
				"./src/*"
			],
			"default": [
				"./src/*"
			]
		}
	},
	"main": "src/worker.ts",
	"types": "src/worker.ts",
	"scripts": {
		"dev": "bun run --env-file=../../.env --watch src/worker.ts",
		"start": "bun run --env-file=../../.env src/worker.ts",
		"test": "bun test --pass-with-no-tests",
		"typecheck": "tsgo --noEmit"
	},
	"dependencies": {
		"@agentic-kanban/core": "workspace:*",
		"@agentic-kanban/queue": "workspace:*",
		"bullmq": "catalog:"
	},
	"devDependencies": {
		"@typescript/native-preview": "catalog:",
		"bun-types": "catalog:"
	}
}
