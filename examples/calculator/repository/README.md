# Calculator

A full-stack calculator application with a Bun backend API and React frontend.

## Running

```bash
bun install
bun run dev          # Start both backend and frontend
bun run dev:backend  # Start backend only (port 3001)
bun run dev:frontend # Start frontend only (Vite dev server)
```

## Testing

```bash
bun test
```
