{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"cli": {
			"cache": false,
			"persistent": true
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"generate": {
			"dependsOn": ["^generate"]
		},
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["src"],
			"outputs": ["dist/**"]
		},
		"typecheck": {
			"inputs": ["src/**/*.ts", "src/**/*.tsx", "tsconfig.json"]
		},
		"test": {}
	}
}
